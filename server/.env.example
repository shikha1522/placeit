# copy this file to .env and fill in your real values
# this file IS committed to GitHub — it shows what variables are needed
# .env is NOT committed — it has your real secrets

PORT=5000
DATABASE_URL=postgresql://YOUR_DB_USER:YOUR_DB_PASSWORD@localhost:5432/placeit
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d